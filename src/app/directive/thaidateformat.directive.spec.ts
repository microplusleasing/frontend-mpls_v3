import { ThaidateformatDirective } from './thaidateformat.directive';

describe('ThaidateformatDirective', () => {
  it('should create an instance', () => {
    const directive = new ThaidateformatDirective();
    expect(directive).toBeTruthy();
  });
});
